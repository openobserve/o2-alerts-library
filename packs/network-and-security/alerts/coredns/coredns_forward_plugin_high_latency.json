{
  "name": "coredns_forward_plugin_high_latency",
  "title": "CoreDNS Forward Plugin High Latency",
  "severity": "critical",
  "description": "CoreDNS 99th percentile latency forwarding requests to upstream is, indicating the upstream resolver is slow or overloaded.",
  "tags": [
    "awesome-prometheus-alerts",
    "coredns",
    "embedded-exporter"
  ],
  "docs_url": "https://samber.github.io/awesome-prometheus-alerts/rules/network-and-security/coredns/",
  "source": {
    "project": "awesome-prometheus-alerts",
    "license": "CC-BY-4.0",
    "url": "https://samber.github.io/awesome-prometheus-alerts/rules/network-and-security/coredns/",
    "exporter": "embedded-exporter",
    "upstream_alert": "CoreDNS Forward Plugin High Latency",
    "upstream_query": "histogram_quantile(0.99, sum(rate(coredns_forward_request_duration_seconds_bucket[5m])) without (pod, instance, rcode)) > 4",
    "upstream_description": "CoreDNS 99th percentile latency forwarding requests to upstream {{ $labels.to }} is {{ $value }}s, indicating the upstream resolver is slow or overloaded."
  },
  "stream_type": "metrics",
  "stream_name": "coredns_forward_request_duration_seconds_bucket",
  "is_real_time": false,
  "query_condition": {
    "type": "promql",
    "conditions": {
      "version": 2,
      "conditions": {
        "filterType": "group",
        "logicalOperator": "AND",
        "conditions": []
      }
    },
    "sql": null,
    "promql": "histogram_quantile(0.99, sum(rate(coredns_forward_request_duration_seconds_bucket[5m])) without (pod, instance, rcode))",
    "promql_condition": {
      "column": "value",
      "operator": ">",
      "value": 4,
      "ignore_case": false
    },
    "aggregation": null,
    "vrl_function": null,
    "search_event_type": null,
    "multi_time_range": []
  },
  "trigger_condition": {
    "period": 10,
    "operator": ">=",
    "threshold": 1,
    "frequency": 5,
    "cron": "",
    "frequency_type": "minutes",
    "silence": 60,
    "timezone": "UTC",
    "tolerance_in_secs": null,
    "align_time": true
  },
  "context_attributes": {},
  "row_template": "*{alert_name}* | to: {to} | value: {value}",
  "row_template_type": "String",
  "enabled": true,
  "tz_offset": 0,
  "creates_incident": false,
  "notes": "4s threshold is a rough default; depends on your upstream resolver's performance and network path — adjust based on your baseline."
}
