{
  "name": "mongodb_replica_set_primary_changed",
  "title": "MongoDB replica set primary changed",
  "severity": "warning",
  "description": "MongoDB member became the replica set primary in the last 10 minutes, which may indicate an election due to failover or instability.",
  "tags": [
    "awesome-prometheus-alerts",
    "mongodb",
    "percona-mongodb-exporter"
  ],
  "docs_url": "https://samber.github.io/awesome-prometheus-alerts/rules/databases/mongodb/",
  "source": {
    "project": "awesome-prometheus-alerts",
    "license": "CC-BY-4.0",
    "url": "https://samber.github.io/awesome-prometheus-alerts/rules/databases/mongodb/",
    "exporter": "percona-mongodb-exporter",
    "upstream_alert": "MongoDB replica set primary changed",
    "upstream_query": "changes(mongodb_rs_myState[10m]) > 0 and mongodb_rs_myState == 1",
    "upstream_description": "MongoDB member {{ $labels.instance }} became the replica set primary in the last 10 minutes, which may indicate an election due to failover or instability.",
    "query_normalized": "The upstream threshold could not be lifted out of the expression (its outermost operator is and/or/unless, or it compares against another series), so the expression is normalised to 1 per matching series and the alert fires on any match. The observed value in the notification is therefore always 1."
  },
  "stream_type": "metrics",
  "stream_name": "mongodb_rs_myState",
  "is_real_time": false,
  "query_condition": {
    "type": "promql",
    "conditions": {
      "version": 2,
      "conditions": {
        "filterType": "group",
        "logicalOperator": "AND",
        "conditions": []
      }
    },
    "sql": null,
    "promql": "(changes(mongodb_rs_myState[10m]) > 0 and mongodb_rs_myState == 1) * 0 + 1",
    "promql_condition": {
      "column": "value",
      "operator": ">=",
      "value": 1,
      "ignore_case": false
    },
    "aggregation": null,
    "vrl_function": null,
    "search_event_type": null,
    "multi_time_range": []
  },
  "trigger_condition": {
    "period": 2,
    "operator": ">=",
    "threshold": 1,
    "frequency": 1,
    "cron": "",
    "frequency_type": "minutes",
    "silence": 60,
    "timezone": "UTC",
    "tolerance_in_secs": null,
    "align_time": true
  },
  "context_attributes": {},
  "row_template": "*{alert_name}* | instance: {instance}",
  "row_template_type": "String",
  "enabled": true,
  "tz_offset": 0,
  "creates_incident": false,
  "notes": "mongodb_rs_myState (this node's replica set state) is exposed in default mode.\nRestricted to == 1 (PRIMARY) so the alert fires once per election, from the new primary's series, instead of on every member state transition (e.g. a secondary's restart/resync cycle).\nOlder exporters (or --compatible-mode) instead expose mongodb_mongod_replset_my_state."
}
