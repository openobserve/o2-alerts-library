{
  "name": "jaeger_storage_completely_unavailable",
  "title": "Jaeger storage completely unavailable",
  "severity": "critical",
  "description": "Jaeger has 100% storage errors — storage backend may be down.",
  "tags": [
    "awesome-prometheus-alerts",
    "embedded-exporter",
    "jaeger"
  ],
  "docs_url": "https://samber.github.io/awesome-prometheus-alerts/rules/observability/jaeger/",
  "source": {
    "project": "awesome-prometheus-alerts",
    "license": "CC-BY-4.0",
    "url": "https://samber.github.io/awesome-prometheus-alerts/rules/observability/jaeger/",
    "exporter": "embedded-exporter",
    "upstream_alert": "Jaeger storage completely unavailable",
    "upstream_query": "sum(rate(jaeger_storage_requests_total{result=\"err\"}[1m])) by (instance, job, namespace, operation) > 0 and sum(rate(jaeger_storage_requests_total{result=\"ok\"}[1m])) by (instance, job, namespace, operation) == 0",
    "upstream_description": "Jaeger on {{ $labels.instance }} has 100% storage errors for {{ $labels.operation }} — storage backend may be down.",
    "query_normalized": "The upstream threshold could not be lifted out of the expression (its outermost operator is and/or/unless, or it compares against another series), so the expression is normalised to 1 per matching series and the alert fires on any match. The observed value in the notification is therefore always 1."
  },
  "stream_type": "metrics",
  "stream_name": "jaeger_storage_requests_total",
  "is_real_time": false,
  "query_condition": {
    "type": "promql",
    "conditions": {
      "version": 2,
      "conditions": {
        "filterType": "group",
        "logicalOperator": "AND",
        "conditions": []
      }
    },
    "sql": null,
    "promql": "(sum(rate(jaeger_storage_requests_total{result=\"err\"}[1m])) by (instance, job, namespace, operation) > 0 and sum(rate(jaeger_storage_requests_total{result=\"ok\"}[1m])) by (instance, job, namespace, operation) == 0) * 0 + 1",
    "promql_condition": {
      "column": "value",
      "operator": ">=",
      "value": 1,
      "ignore_case": false
    },
    "aggregation": null,
    "vrl_function": null,
    "search_event_type": null,
    "multi_time_range": []
  },
  "trigger_condition": {
    "period": 2,
    "operator": ">=",
    "threshold": 1,
    "frequency": 1,
    "cron": "",
    "frequency_type": "minutes",
    "silence": 60,
    "timezone": "UTC",
    "tolerance_in_secs": null,
    "align_time": true
  },
  "context_attributes": {},
  "row_template": "*{alert_name}* | instance: {instance} | operation: {operation}",
  "row_template_type": "String",
  "enabled": true,
  "tz_offset": 0,
  "creates_incident": false,
  "notes": "Fires when all storage operations for a given type are failing and none are succeeding.\nIndicates the storage backend (Cassandra, Elasticsearch, etc.) is likely unreachable or misconfigured."
}
