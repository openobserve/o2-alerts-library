{
  "name": "jaeger_service_discovery_errors",
  "title": "Jaeger service discovery errors",
  "severity": "warning",
  "description": "Jaeger is returning a high percentage HTTP 5xx errors on the services endpoint.",
  "tags": [
    "awesome-prometheus-alerts",
    "embedded-exporter",
    "jaeger"
  ],
  "docs_url": "https://samber.github.io/awesome-prometheus-alerts/rules/observability/jaeger/",
  "source": {
    "project": "awesome-prometheus-alerts",
    "license": "CC-BY-4.0",
    "url": "https://samber.github.io/awesome-prometheus-alerts/rules/observability/jaeger/",
    "exporter": "embedded-exporter",
    "upstream_alert": "Jaeger service discovery errors",
    "upstream_query": "100 * sum(rate(http_server_request_duration_seconds_count{http_route=\"/api/services\",http_response_status_code=~\"5..\"}[1m])) by (instance, job, namespace) / sum(rate(http_server_request_duration_seconds_count{http_route=\"/api/services\"}[1m])) by (instance, job, namespace) > 1 and sum(rate(http_server_request_duration_seconds_count{http_route=\"/api/services\"}[1m])) by (instance, job, namespace) > 0",
    "upstream_description": "Jaeger on {{ $labels.instance }} is returning {{ $value | humanize }}% HTTP 5xx errors on the services endpoint.",
    "query_normalized": "The upstream threshold could not be lifted out of the expression (its outermost operator is and/or/unless, or it compares against another series), so the expression is normalised to 1 per matching series and the alert fires on any match. The observed value in the notification is therefore always 1."
  },
  "stream_type": "metrics",
  "stream_name": "http_server_request_duration_seconds_count",
  "is_real_time": false,
  "query_condition": {
    "type": "promql",
    "conditions": {
      "version": 2,
      "conditions": {
        "filterType": "group",
        "logicalOperator": "AND",
        "conditions": []
      }
    },
    "sql": null,
    "promql": "(100 * sum(rate(http_server_request_duration_seconds_count{http_route=\"/api/services\",http_response_status_code=~\"5..\"}[1m])) by (instance, job, namespace) / sum(rate(http_server_request_duration_seconds_count{http_route=\"/api/services\"}[1m])) by (instance, job, namespace) > 1 and sum(rate(http_server_request_duration_seconds_count{http_route=\"/api/services\"}[1m])) by (instance, job, namespace) > 0) * 0 + 1",
    "promql_condition": {
      "column": "value",
      "operator": ">=",
      "value": 1,
      "ignore_case": false
    },
    "aggregation": null,
    "vrl_function": null,
    "search_event_type": null,
    "multi_time_range": []
  },
  "trigger_condition": {
    "period": 5,
    "operator": ">=",
    "threshold": 1,
    "frequency": 1,
    "cron": "",
    "frequency_type": "minutes",
    "silence": 60,
    "timezone": "UTC",
    "tolerance_in_secs": null,
    "align_time": true
  },
  "context_attributes": {},
  "row_template": "*{alert_name}* | instance: {instance}",
  "row_template_type": "String",
  "enabled": true,
  "tz_offset": 0,
  "creates_incident": false,
  "notes": "Errors on /api/services indicate the storage backend cannot return the list of instrumented services,\nwhich breaks the Jaeger UI service selector."
}
