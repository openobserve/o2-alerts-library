{
  "name": "spinnaker_clouddriver_high_error_rate",
  "title": "Spinnaker Clouddriver high error rate",
  "severity": "warning",
  "description": "Clouddriver 5xx error rate is. Cloud operations may be failing.",
  "tags": [
    "awesome-prometheus-alerts",
    "embedded-exporter",
    "spinnaker"
  ],
  "docs_url": "https://samber.github.io/awesome-prometheus-alerts/rules/ci-cd/spinnaker/",
  "source": {
    "project": "awesome-prometheus-alerts",
    "license": "CC-BY-4.0",
    "url": "https://samber.github.io/awesome-prometheus-alerts/rules/ci-cd/spinnaker/",
    "exporter": "embedded-exporter",
    "upstream_alert": "Spinnaker Clouddriver high error rate",
    "upstream_query": "sum by (instance) (rate(controller_invocations_total{status=\"5xx\", job=~\".*clouddriver.*\"}[5m])) / sum by (instance) (rate(controller_invocations_total{job=~\".*clouddriver.*\"}[5m])) > 0.05 and sum by (instance) (rate(controller_invocations_total{job=~\".*clouddriver.*\"}[5m])) > 0",
    "upstream_description": "Clouddriver 5xx error rate is {{ $value | humanizePercentage }} on {{ $labels.instance }}. Cloud operations may be failing.",
    "query_normalized": "The upstream threshold could not be lifted out of the expression (its outermost operator is and/or/unless, or it compares against another series), so the expression is normalised to 1 per matching series and the alert fires on any match. The observed value in the notification is therefore always 1."
  },
  "stream_type": "metrics",
  "stream_name": "controller_invocations_total",
  "is_real_time": false,
  "query_condition": {
    "type": "promql",
    "conditions": {
      "version": 2,
      "conditions": {
        "filterType": "group",
        "logicalOperator": "AND",
        "conditions": []
      }
    },
    "sql": null,
    "promql": "(sum by (instance) (rate(controller_invocations_total{status=\"5xx\", job=~\".*clouddriver.*\"}[5m])) / sum by (instance) (rate(controller_invocations_total{job=~\".*clouddriver.*\"}[5m])) > 0.05 and sum by (instance) (rate(controller_invocations_total{job=~\".*clouddriver.*\"}[5m])) > 0) * 0 + 1",
    "promql_condition": {
      "column": "value",
      "operator": ">=",
      "value": 1,
      "ignore_case": false
    },
    "aggregation": null,
    "vrl_function": null,
    "search_event_type": null,
    "multi_time_range": []
  },
  "trigger_condition": {
    "period": 5,
    "operator": ">=",
    "threshold": 1,
    "frequency": 1,
    "cron": "",
    "frequency_type": "minutes",
    "silence": 60,
    "timezone": "UTC",
    "tolerance_in_secs": null,
    "align_time": true
  },
  "context_attributes": {},
  "row_template": "*{alert_name}* | instance: {instance}",
  "row_template_type": "String",
  "enabled": true,
  "tz_offset": 0,
  "creates_incident": false
}
